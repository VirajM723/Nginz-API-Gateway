worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Logging format with request ID
    log_format nginz_log '$remote_addr - $remote_user [$time_local] "$request" '
                        '$status $body_bytes_sent "$http_referer" '
                        '"$http_user_agent" "$http_x_request_id"';

    access_log /var/log/nginx/access.log nginz_log;
    error_log /var/log/nginx/error.log warn;

    upstream gateway_upstream {
        server gateway:3000;
    }

    server {
        listen 80;
        server_name localhost;

        # API Proxying
        location /api/ {
            proxy_pass http://gateway_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Request-ID $http_x_request_id;
        }

        # Health check proxy
        location /health {
            proxy_pass http://gateway_upstream/health;
        }

        # Metrics proxy
        location /metrics {
            proxy_pass http://gateway_upstream/metrics;
        }

        # Static Frontend Fallback
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
